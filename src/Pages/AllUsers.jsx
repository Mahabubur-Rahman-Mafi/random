
import React from "react";
import { Button } from "react-bootstrap";
import { toast } from "react-hot-toast";
const AllUsers = ({ u, idx, }) => {
  const {
    firstName,
    lastName,
    } = u;
    

    console.log(u);
    <>
     
    </>;
};

export default AllUsers;
